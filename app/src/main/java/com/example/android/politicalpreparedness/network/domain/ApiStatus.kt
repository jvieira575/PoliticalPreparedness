package com.example.android.politicalpreparedness.network.domain

/**
 * Enum used to display progress bar information when loading asteroids from the network.
 */
enum class ApiStatus {
    LOADING,
    ERROR,
    DONE
}